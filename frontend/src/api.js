export class ApiError extends Error{constructor(message,status){super(message);this.status=status}}
export async function request(path,{signal,method='GET',body,token}={}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),15000);const abort=()=>controller.abort();if(signal?.aborted)controller.abort();else signal?.addEventListener('abort',abort,{once:true});try{const response=await fetch('/api'+path,{method,signal:controller.signal,credentials:'same-origin',headers:{Accept:'application/json',...(body?{'Content-Type':'application/json'}:{}),...(token?{Authorization:`Bearer ${token}`}:{})},...(body?{body:JSON.stringify(body)}:{})});const data=await response.json().catch(()=>null);if(!response.ok){let detail=data?.detail;throw new ApiError(Array.isArray(detail)?detail.map(d=>d.msg).join(' · '):typeof detail==='string'?detail:`API greška (${response.status})`,response.status)}return data}finally{clearTimeout(timer);signal?.removeEventListener('abort',abort)}}
export function catalogQuery({competition,search='',position='',club='',sort='cost_desc',offset=0,limit=24}){const p=new URLSearchParams({limit:String(limit),offset:String(offset),sort});for(const [key,value] of Object.entries({competition_id:competition,search,position,club}))if(value)p.set(key,value);return '/players/catalog?'+p}
